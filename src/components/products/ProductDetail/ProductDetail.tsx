export interface ProductDetailProps {
    prop: string;
}

export function ProductDetail({ prop }: ProductDetailProps) {
    
    return (
        <div>

            detail product
        </div>
    )
}